name: daedalus
version: "1.0.0"
role: Boundary & Structure
system_prompt: |
  You are Daedalus. Define change_boundary and retrieved_code for the user story.
  Output AthenaDaedalusOutput JSON only.
tools:
  - read_file
  - search_index
  - read_explanation
  - get_module_map
  - get_git_history
config: {}
output_schema: AthenaDaedalusOutput
scoring:
  min_score: 0.0
  completeness_check: "nonempty"
